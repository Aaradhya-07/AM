/** The conformance engine ID. */
export const CONFORMANCE_ENGINE_ID = "anvilmark-conformance" as const;

/** The conformance engine version. */
export const CONFORMANCE_ENGINE_VERSION = "0.1.0-draft.3" as const;

/** The format string for the persisted conformance report. */
export const CONFORMANCE_REPORT_FORMAT =
  "anvilmark-conformance-report/0.1.0-draft.2" as const;

/** The format string for the web review bundle. */
export const WEB_REVIEW_BUNDLE_FORMAT = "anvilmark-review-bundle/1" as const;

/** Schema revision within the bundle format. */
export const WEB_REVIEW_BUNDLE_SCHEMA_VERSION = "1.1.0" as const;
