# Hardware sizing — resumed and completed

The user resumed work after resetting usage. The supported implementation and remaining verification from the earlier pause are complete on `emergent-playground` in `/Users/na61/Desktop/ANVILMARK-milestone-5`.

See [the final implementation record](hardware-sizing-implementation.md) for delivered features, actual verification, and explicit scope limits. Changes remain uncommitted. No deployment, model download, or mutation of a real project contract was performed; contract-write tests used temporary fixture projects.

The staging directory `/private/tmp/anvilmark-hardware-next` is historical working material. Do not reapply its old manifests or scripts over the current checkout. Preserve the pre-existing untracked `reference/`, `claude-review-and-critique-brief.md`, and `codex-web-plan/` files.
